import { Component } from '@angular/core';

/**
 * decorator > metadata
 * controller
 * api/account
 * api/order
 * selector is same as controller path
 * ./ root
 */
@Component({
  selector: 'app-header',
  templateUrl: './header.component.html',
  styleUrls: ['./header.component.css']
})
export class HeaderComponent {
  
}
